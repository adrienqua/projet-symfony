{% extends 'base.html.twig' %}

{% block title %}Create Category
{% endblock %}

{% block content %}
	<div class="container mx-auto px-4 py-8">
		<h1 class="text-2xl font-bold mb-6 text-gray-800 dark:text-white">Créer une nouvelle catégorie</h1>

		<div class="bg-white dark:bg-gray-800 p-6 rounded-lg shadow-md">
			{{ include('admin/category/_form.html.twig', { button_label: 'Créer' }) }}
		</div>

		<div class="mt-6">
			<a href="{{ path('app_category_index') }}" class="text-blue-600 dark:text-blue-400 hover:underline">← Revenir aux catégories</a>
		</div>
	</div>
{% endblock %}
