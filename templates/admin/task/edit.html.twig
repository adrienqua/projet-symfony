{% extends 'base.html.twig' %}
{% block title %}Edit Task
{% endblock %}
{% block content %}
	<div class="container mx-auto my-8">
		<h1 class="text-3xl font-bold text-gray-800 dark:text-white mb-6">Modifier une tâche</h1>
		<div class="bg-white dark:bg-gray-800 shadow-md rounded-lg p-6 mb-6">
			{{ include('admin/task/_form.html.twig', { 'button_label': 'Modifier' }) }}
		</div>
		<div class="flex justify-between items-center">
			<a href="{{ path('app_task_index') }}" class="text-blue-500 hover:underline">
				← Revenir aux tâches
			</a>
			<div>
				{{ include('admin/task/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}
