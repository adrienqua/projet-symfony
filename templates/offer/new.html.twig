{% extends 'base.html.twig' %}

{% block title %}Create Offer{% endblock %}

{% block content %}
<div class="container mx-auto my-8">
    <h1 class="text-3xl font-bold text-gray-800 dark:text-white mb-6">Créer une nouvelle offre</h1>

    <div class="bg-white dark:bg-gray-800 shadow-md rounded-lg p-6">
        {{ include('offer/_form.html.twig', { 'button_label': 'Créer' }) }}
    </div>

    <div class="mt-6">
        <a href="{{ path('app_offer_index') }}" class="text-blue-500 hover:underline">
            ← Revenir aux offres
        </a>
    </div>
</div>
{% endblock %}
